# --- OS / editor junk ---
.DS_Store
.AppleDouble
Thumbs.db
.vscode/
.idea/
*.swp
*~

# --- Internal strategy: not for the public repo by default ---
# Remove this line if you deliberately want the rollout plan versioned/published.
docs/adoption-roadmap.md

# --- Secrets & credentials: NEVER commit these ---
.env
.env.*
*.key
*.pem
*.p12
*.pfx
*.keystore
id_rsa
id_ed25519
secrets.*
*.secret
*credentials*
*token*
service-account*.json
